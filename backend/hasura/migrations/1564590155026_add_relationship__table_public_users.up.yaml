- args:
    name: users_x_roles
    table:
      name: users
      schema: public
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: users_x_roles
          schema: public
  type: create_array_relationship
